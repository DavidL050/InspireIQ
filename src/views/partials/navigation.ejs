<!-- NAVBAR -->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark w-100">
    <div class="container-fluid">
        <div class="navbar-brand-container">
            <img src="https://i.ibb.co/q5M3fqY/Logo-white.png" alt="InspireIQ Logo" class="logo" />
            <a class="navbar-brand" href="/">InspireIQ</a>
        </div>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-between align-items-center" id="navbarNav">
            <div class="search-container mx-auto">
                <input type="text" class="form-control search-bar" placeholder="Buscar cursos gratuitos" />
                <a href="/course" class="search-icon-link">
                    <span class="material-icons search-icon">search</span>
                </a>
            </div>
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="/">Inicio</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/course">Cursos</a>
                </li>

                <!-- Mostrar u ocultar botones basados en la sesión del usuario -->
                <% if (!userId) { %>
                    <li class="nav-item">
                        <button class="btn-signup"><a class="nav-link" data-bs-toggle="modal" data-bs-target="#signupModal" style="color: rgb(255, 255, 255); font-weight:400;">Registrarse</a></button>
                    </li>
                    <li class="nav-item">
                        <button class = "btn-login">
                            <a class="nav-link" data-bs-toggle="modal" data-bs-target="#signinModal" style="color: white; font-weight: 400;">Iniciar Sesión</a>
                        </button>
                    </li>
                <% } else { %>
                    <li class="nav-item">
                        <a class="nav-link" href="/profile">Perfil</a>
                    </li>
                    <% if (userRole === 'teacher') { %>
                        <li class="nav-item">
                            <a class="nav-link" href="/create_course">Crear Curso</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/my_courses">Mis Cursos</a>
                        </li>
                    <% } %>
                    <li class="nav-item">
                        <a class="nav-link" href="/logout">Cerrar Sesión</a>
                    </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>
